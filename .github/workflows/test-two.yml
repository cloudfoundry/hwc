name: Test 2
on:
  push:
    branches:
      - migrate-pipelines
    paths-ignore: [ 'README.md' ]
jobs:
  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Tests
        shell: powershell
        run: |
          ./scripts/run_test.ps1